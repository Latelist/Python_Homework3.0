# Задайте список из нескольких чисел. Напишите программу, которая 
# найдёт сумму элементов списка, стоящих на нечётных идексах.
# Пример:
# [2, 3, 5, 9, 3] -> на нечётных идексах элементы 3 и 9, ответ: 12

list = [35, 10, 96, 21, 546, 423, 78, 14]

def sum_odd_index(list):
    sum = 0
    for i in range(1, len(list), 2):
        sum += list[i]
    
    return sum

print(sum_odd_index(list))